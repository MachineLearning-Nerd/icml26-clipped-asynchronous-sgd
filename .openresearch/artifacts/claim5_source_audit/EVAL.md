# Claim 5 evaluation

Status: **FALSIFIED**  
Confidence: **MEDIUM**

Under the natural carried-comparator reading, Shakespeare is compared to
Vanilla ASGD. The exact source reports `1.8x` at `D=4` and `2.0x` at `D=8`,
not the imported `2.0-2.2x` range. The CIFAR conjunct matches. Because both
conjuncts are required, the compound imported claim is false under this
contract.

The remaining risk is explicit: supplying Delay-adaptive ASGD as a new,
unstated comparator changes the source values to `2.1x` and `2.2x`.
