# Method

This route treats the imported Claim 5 as a compound textual claim and checks it
against the exact paper source. The only explicit comparator is carried across
the two coordinated clauses. An independent checker recomputes range membership
for each delay and tests corrected-comparator controls.

No empirical run is needed to decide whether the imported numerical statement
matches its own primary source.
