# Method

The printed bounded-increment constant is replaced by the value implied by its
premises. Freedman's free parameter is then changed to preserve one-quarter
descent absorption. All horizon normalizations and simultaneous tail events
are retained. The resulting residual is inverted independently rather than
discarded or selected to match the headline.
