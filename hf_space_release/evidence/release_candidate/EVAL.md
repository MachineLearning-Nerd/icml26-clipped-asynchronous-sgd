# Evaluation

The release candidate passes only if `verify.py` exits zero. A passing release
gate is evidence that the candidate is internally consistent and discoverable;
it is not a new judge score. The live score remains 4/12 until a new verdict is
recorded.
